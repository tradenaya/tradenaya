services:
  mysql:
    image: mysql:8
    container_name: dfly-mysql
    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: root123

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: